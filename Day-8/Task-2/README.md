### **2️⃣ Student Name Display**  
- Create a new React component called `StudentName`.  
- This component should accept a prop called `name`.  
- Display "Student Name: " followed by the value of the `name` prop.
